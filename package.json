{
  "name": "travis-shields-docs",
  "version": "1.0.0",
  "description": "A sample project for testing and configuring TravisCI with mocha/chai and status shields",
  "main": "index.js",
  "author": "Frédérique Mittelstaedt <git@gfm.io> (www.gfm.io)",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "mocha -r ts-node/register tests/**/*.test.ts",
    "test:coverage": "nyc -r lcov -e .ts -x \"*.test.ts\" mocha -r ts-node/register tests/**/*.test.ts && nyc report"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.6",
    "chai": "^4.2.0",
    "mocha": "^6.0.2",
    "nyc": "^13.3.0",
    "ts-node": "^8.0.2",
    "tslint": "^5.13.1",
    "typescript": "^3.3.3333"
  }
}
